apiVersion: v1
kind: Service
metadata:
  name: agent-api
  namespace: hello-xiao-a
  labels:
    app: agent-api
spec:
  type: ClusterIP
  selector:
    app: agent-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
