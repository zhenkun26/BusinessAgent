# ============================================
# Hello，智多星——企业知识工作流 Agent - 依赖清单
# 用法: pip install -r requirements.txt
# 或仅装最小集跑通 W4 评测: pip install -r requirements.txt --no-deps -e .
# ============================================

# ---- 核心基础(任何阶段都需要) ----
loguru>=0.7
pydantic>=2.0
pydantic-settings>=2.0
python-dotenv>=1.0
numpy>=1.26

# ---- LangChain 生态 ----
langchain>=1.0
langchain-core>=1.0
langchain-community>=0.4
langchain-text-splitters>=1.0
langchain-openai>=1.0          # DeepSeek / OpenAI 兼容协议
langchain-huggingface>=1.0     # 本地 bge-m3 embedding
langchain-ollama>=1.0          # 本地 Ollama(ChatOllama,reasoning=False 关思考)
langchain-cohere>=0.3.0          # Cohere Reranker(可选)

# ---- LangGraph ----
langgraph>=1.2
langgraph-checkpoint-redis>=0.5
langsmith>=0.1.100

# ---- 向量库 ----
pymilvus>=2.4.0,<3.1             # 生产用(memory 后端不需要);3.1 移除 ORM API,迁移前钉死

# ---- 本地 Embedding(用 local provider 时需要) ----
sentence-transformers>=3.0.0
torch>=2.2.0

# ---- Web 框架 ----
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# ---- 数据库 ----
sqlalchemy>=2.0
asyncpg>=0.29
redis>=5.0

# ---- HTTP 客户端 ----
httpx>=0.27

# ---- 安全 ----
PyJWT>=2.8
passlib[bcrypt]>=1.7.4
circuitbreaker>=2.0

# ---- 文档处理 ----
pypdf>=4.0
python-docx>=1.1
unstructured>=0.15

# ---- 可观测性 ----
opentelemetry-api>=1.25
opentelemetry-sdk>=1.25
opentelemetry-instrumentation-fastapi>=0.45
prometheus-client>=0.20

# ---- 工具 ----
tenacity>=8.2

# ---- 评测(可选,eval extra) ----
# datasets>=2.20
# ragas>=0.1
# langchain-evaluation>=0.3

# ---- 开发工具(可选,dev extra) ----
# pytest>=8.0
# pytest-asyncio>=0.23
# pytest-cov>=5.0
# ruff>=0.6
# mypy>=1.10
