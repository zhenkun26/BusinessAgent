## 1. 答案层评测能力建设

- [x] 1.1 扩展 `eval/run_eval.py`：在检索后对每条样本调用生成链路（`app/rag/`），统计 expected_keywords 在最终答案中的覆盖率，生成失败样本标记状态且不计入分子分母
- [x] 1.2 扩展 EvalReport 与结果落盘：`eval/results/latest.json` 增加答案层覆盖率字段（总体与分场景），与检索层指标并列，保持既有字段兼容
- [x] 1.3 增加逐样本诊断输出：检索命中情况、进入上下文的内容、生成答案，支撑失效归因

## 2. 失效模式分析（现有 22 条）

- [x] 2.1 对现有 22 条评测集跑一轮含答案层的评测，导出低覆盖样本清单（当前检索层覆盖率 < 1.0 的样本，含 Q018 覆盖率 0）
- [x] 2.2 按三分类（检索未命中 / 上下文组装遗漏 / 生成遗漏）逐条归因，人工抽查区分"真漏答"与"关键词口径误伤"
- [x] 2.3 输出失效模式分类统计与修复建议，结论追加到 `docs/40-process/DECISIONS.md` 或 ISSUES.md（按内容性质选择）

## 3. 评测集扩充至 ≥100 条

- [x] 3.1 盘点 `eval/sample_docs/` 现有 5 份文档与 dept_sales/dept_finance 部门目录可支撑的样本量，确定需新增的样例文档（人事、IT 等业务域）
- [x] 3.2 补充样例文档并入库验证检索可达
- [x] 3.3 编写新样本至总数 ≥100 条，沿用现有字段格式，保持 scene/difficulty 分层均衡，单一文档样本占比 ≤50%
- [x] 3.4 编写对抗性样本（库内无答案、诱导性表述、跨文档易混淆），标注预期行为（拒答/澄清），并确定拒答判定口径（design Open Questions 第 1 条）
- [x] 3.5 全量跑通评测，确认评测集规模、分层与多域覆盖满足规格要求

## 4. 修复与回归基线

- [x] 4.1 按失效模式分析结论修复 prompt 模板或上下文组装策略（仅针对占主导的失效类别）
- [x] 4.2 对完整评测集执行回归评测，答案层平均关键词覆盖率达到 ≥0.85
- [x] 4.3 将本轮实测结果归档为回归基线（`eval/results/` 留档），明确后续 RAG 变更的回归要求
- [x] 4.4 运行 `openspec validate rag-answer-quality --strict` 通过后归档
