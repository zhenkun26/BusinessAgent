# 企业知识工作流 Agent — 阶段性总结（W2 → W6）

> 生成时间：2026-07-26
> 当前进度：W6 完成（Redis Checkpointer 断点恢复 + interrupt 人工介入验证通过）
> 后续进展:W7-W9 + 联调 + P1/P2 迭代 + 前端/流式增强均已完成,见 [阶段性总结 W7-W9+联调](阶段性总结_W7-W9_联调.md)
> 架构基础：LangChain 0.3 + LangGraph 1.2 + Milvus + Redis + PostgreSQL

---

## 一、整体进度概览

| 阶段 | 主题 | 状态 | 核心交付 |
|------|------|------|----------|
| W2 | 基础设施搭建 | ✅ 完成 | Docker 服务、本地 Embeddings、配置体系 |
| W3 | RAG 检索 + KnowledgeAgent | ✅ 完成 | 两阶段检索、三级降级、置信度决策 |
| W4 | 评估体系 | ✅ 完成 | eval_set、run_eval、命中率/覆盖率指标 |
| W5 | 多 Agent 并行图 | ✅ 完成 | LangGraph Send API、Planner→Executor→Aggregator |
| W6 | Checkpointer 断点恢复 | ✅ 完成 | Redis 三级降级链、跨进程状态恢复、interrupt |
| W7 | ExecutionAgent（外部业务系统对接） | ⏳ 待启动 | CRM/邮件/工单集成、Saga 补偿 |
| W8+ | API 层、安全、可观测性 | ⏳ 待启动 | JWT 刷新、批量审批、OpenTelemetry |

**总体完成度：** 5/8（约 62%），核心知识检索 + 多 Agent 协作 + 状态持久化链路已打通。

---

## 二、各阶段详细总结

### W2：基础设施搭建 ✅

**目标：** 建立可运行的开发环境，选型并落地底层依赖。

**关键决策：**
- **VectorStore 后端：** Docker 启动 Milvus（生产）+ InMemory Mock（开发期，无需 Docker）
- **Embedding：** 本地 HuggingFace `bge-m3`（1024 维），路径 `D:/models/bge-m3`，device=cuda
- **Reranker：** 本地 `bge-reranker-large`，免费无 API 依赖
- **LLM 分层：**
  - Primary：DeepSeek v4-pro（云端，复杂推理/答案生成）
  - Lite：DeepSeek v4-flash（云端，意图分类）
  - Local：qwen3.5:4b（Ollama 本地，闲聊判断/答案自评/Reranker 降级）

**交付物：**
- [docker-compose.yml](file:///d:/Project/企业agent/enterprise-agent/docker-compose.yml) — Milvus + PostgreSQL + Redis + Ollama 编排
- [app/config.py](file:///d:/Project/企业agent/enterprise-agent/app/config.py) — Settings 单例，环境变量驱动
- [app/core/milvus_client.py](file:///d:/Project/企业agent/enterprise-agent/app/core/milvus_client.py) — Milvus 连接 + Collection Schema
- [.env](file:///d:/Project/企业agent/enterprise-agent/.env) — 本地实际配置（含强密码）

**关键问题与修复：**
- Conda 环境需 Python ≥ 3.11（PEP 604 类型语法 + LangChain 0.3+ 兼容）
- 用户 Python 3.10.11 不足，创建 `enterprise_agent` 环境（Python 3.11）

---

### W3：RAG 检索 + KnowledgeAgent ✅

**目标：** 实现企业知识库的两阶段检索 + 置信度决策 + LLM 答案生成。

**核心架构：**

```
用户查询
  ↓
[EnterpriseRAGRetriever]
  ├─ 粗排：向量召回 Top-K（扩大候选池 ×4）
  ├─ 精排：BGE Reranker 重排 Top-N
  └─ 场景识别（POLICY/FACTUAL/COMPARATIVE/MULTI_HOP/OPERATIONAL）
  ↓
[DegradationChain] 三级降级
  ├─ Level 1：HNSW 向量检索
  ├─ Level 2：BM25 关键词（向量失败时）
  └─ Level 3：PostgreSQL LIKE（BM25 失败时）
  ↓
[ConfidenceDecider] 场景化阈值
  ├─ 综合分 = 检索分 × 0.6 + LLM 自评分 × 0.4
  └─ 决策：answer / answer_with_hint / answer_skip_hint / reject
  ↓
[KnowledgeAgent] LLM 生成答案
  ├─ Primary LLM 生成答案（带来源标注）
  ├─ Local LLM 自评（qwen3.5:4b，省云端成本）
  └─ 输出 AgentResult（含 needs_replan 字段触发重规划）
```

**交付物：**
- [app/rag/retriever.py](file:///d:/Project/企业agent/enterprise-agent/app/rag/retriever.py) — 两阶段检索器
- [app/rag/degradation.py](file:///d:/Project/企业agent/enterprise-agent/app/rag/degradation.py) — 三级降级链
- [app/rag/confidence.py](file:///d:/Project/企业agent/enterprise-agent/app/rag/confidence.py) — 场景化阈值决策
- [app/rag/vector_store.py](file:///d:/Project/企业agent/enterprise-agent/app/rag/vector_store.py) — VectorStore 抽象 + Milvus/InMemory 实现
- [app/rag/llm.py](file:///d:/Project/企业agent/enterprise-agent/app/rag/llm.py) — LLM 路由（primary/lite/local）
- [app/rag/embeddings.py](file:///d:/Project/企业agent/enterprise-agent/app/rag/embeddings.py) — 本地 bge-m3
- [app/rag/reranker.py](file:///d:/Project/企业agent/enterprise-agent/app/rag/reranker.py) — 本地 BGE Reranker
- [app/rag/ingest.py](file:///d:/Project/企业agent/enterprise-agent/app/rag/ingest.py) — 文档入库服务
- [app/agents/knowledge.py](file:///d:/Project/企业agent/enterprise-agent/app/agents/knowledge.py) — KnowledgeAgent

**关键问题与修复：**
- **命名空间过滤过严：** 用户 `dept_sales` 无法访问 `shared_company` 文档
  - 修复：`_match_filter` 支持「本部门 + 公司共享」双匹配
- **本地小模型未充分利用：** 仅在 lite 降级时使用
  - 修复：新增 3 个本地小模型使用点（闲聊判断、答案自评、LLM Reranker 降级）

---

### W4：评估体系 ✅

**目标：** 建立 RAG 质量评估框架，量化检索命中率与答案覆盖率。

**交付物：**
- [eval/eval_set.json](file:///d:/Project/企业agent/enterprise-agent/eval/eval_set.json) — 评估数据集（含预期答案/来源）
- [eval/run_eval.py](file:///d:/Project/企业agent/enterprise-agent/eval/run_eval.py) — 评估脚本
- [eval/results/latest.json](file:///d:/Project/企业agent/enterprise-agent/eval/results/latest.json) — 最新评估结果
- [eval/sample_docs/](file:///d:/Project/企业agent/enterprise-agent/eval/sample_docs) — 样本文档（财务报销制度、销售政策）

**评估指标：**
- 检索命中率（Top-K 召回率）
- 答案覆盖率（FULL / PARTIAL / NONE）
- 综合置信度分布
- 端到端延迟

---

### W5：多 Agent 并行图 ✅

**目标：** 用 LangGraph StateGraph 建模业务流程，支持任务分解 + 并行 fan-out + 结果汇总。

**图结构：**

```
START
  ↓
planner（意图分类 + 任务分解）
  ↓ (route_after_planner 条件边)
  ├─ "aggregator"（闲聊/无子任务） → aggregator → END
  └─ list[Send("agent_executor", sub_state)]（多子任务，并行 fan-out）
       ↓
     agent_executor（并行 N 个）
       ↓
     aggregator → END
```

**关键设计：**
- **Send API 正确用法：** Send 必须在条件边函数返回，不能在节点返回
- **条件边 path_map：** `add_conditional_edges` 第三参数指定可路由目标节点
- **本地小模型优化：** 闲聊快速判断（超短消息 + 关键词检测 + 本地 LLM 兜底），跳过云端分类
- **WorkflowState：** 含 `parallel_results` reducer 支持 fan-out 结果合并

**交付物：**
- [app/graph/state.py](file:///d:/Project/企业agent/enterprise-agent/app/graph/state.py) — AgentState TypedDict + make_initial_state
- [app/graph/planner.py](file:///d:/Project/企业agent/enterprise-agent/app/graph/planner.py) — 意图分类 + 任务分解
- [app/graph/dispatcher.py](file:///d:/Project/企业agent/enterprise-agent/app/graph/dispatcher.py) — 条件路由（返回 Send 列表）
- [app/graph/executor.py](file:///d:/Project/企业agent/enterprise-agent/app/graph/executor.py) — Agent 执行器
- [app/graph/aggregator.py](file:///d:/Project/企业agent/enterprise-agent/app/graph/aggregator.py) — 结果汇总
- [app/graph/graph.py](file:///d:/Project/企业agent/enterprise-agent/app/graph/graph.py) — 主图组装
- [eval/run_w5_e2e.py](file:///d:/Project/企业agent/enterprise-agent/eval/run_w5_e2e.py) — 端到端验证

**关键问题与修复：**
- **Planner LLM 分类失败：** prompt 中 JSON 示例 `{}` 与 `.format()` 冲突
  - 修复：转义为 `{{}}`
- **dispatcher 返回 Send 报错：** Send 在节点返回
  - 修复：去掉 dispatcher 节点，改用 `add_conditional_edges` 返回 `list[Send]`

**验证结果：**
- 闲聊场景：本地小模型判定 → 直接 aggregator（0ms 延迟）
- 单知识问答：planner → knowledge → aggregator（RAG 粗排 6 条、精排 5 条，Top score 0.705）
- 多任务并行：planner 分解 2 子任务 → fan-out → aggregator 汇总

---

### W6：Checkpointer 断点恢复 ✅

**目标：** 接入 Redis Checkpointer，实现长流程中断恢复 + 跨进程状态持久化 + interrupt 人工介入。

**三级降级链设计：**

```
get_checkpointer()
  ↓
Level 1: AsyncRedisSaver（主路径，生产推荐）
  ├─ 持久化到 Redis（含 RediSearch 模块）
  ├─ 跨进程状态恢复
  └─ 支持 interrupt
  ↓ 失败
Level 2: PostgresSaver（PG 持久化）
  ├─ langgraph 1.x 自动用 to_thread 包装 sync 方法
  └─ 跨进程状态恢复
  ↓ 失败
Level 3: MemorySaver（进程内内存，重启丢失）
  └─ 仅开发/兜底场景
```

**交付物：**
- [app/graph/checkpointer.py](file:///d:/Project/企业agent/enterprise-agent/app/graph/checkpointer.py) — 三级降级链 + 单例缓存
- [app/graph/graph.py](file:///d:/Project/企业agent/enterprise-agent/app/graph/graph.py) — `run_graph(thread_id, use_checkpointer)` 增强
- [eval/run_w6_checkpoint.py](file:///d:/Project/企业agent/enterprise-agent/eval/run_w6_checkpoint.py) — 4 项验证脚本
- [docker-compose.yml](file:///d:/Project/企业agent/enterprise-agent/docker-compose.yml) — redis-stack-server + 显式 loadmodule

**关键问题与修复：**
- **Redis `FT._LIST` 报 unknown command：** `redis:7-alpine` 不含 RediSearch 模块
  - 修复：换用 `redis/redis-stack-server:7.4.0-v3`
- **模块未加载：** 覆盖 `command:` 跳过了镜像默认的模块加载
  - 修复：显式 `--loadmodule redisearch.so` + `rejson.so`
- **alist 健康检查类型错误：** `before=False` 类型不匹配 + alist 返回 AsyncIterator 不能直接 await
  - 修复：移除额外 ping，`asetup` 成功即视为可用

**验证结果（4/4 PASS）：**

| 测试 | 结果 | 关键证据 |
|------|------|----------|
| Checkpointer 初始化 | ✓ PASS | 后端=redis，主路径可用 |
| 状态持久化 | ✓ PASS | checkpoint_id 写入 redis，step=3 |
| 跨进程恢复 | ✓ PASS | 进程A 回答 = 进程B 恢复回答 |
| interrupt 人工介入 | ✓ PASS | 暂停于 `next=('ask',)`，Command(resume='yes') 后 `confirmed=True` |

---

## 三、技术栈与依赖

### 已落地依赖

| 类别 | 组件 | 版本 | 用途 |
|------|------|------|------|
| 编排 | langgraph | 1.2.9 | StateGraph + Send + Checkpointer |
| 编排 | langgraph-checkpoint-redis | 0.5.1 | AsyncRedisSaver |
| 编排 | langgraph-checkpoint-postgres | 3.1.0 | PostgresSaver 降级 |
| 抽象 | langchain | 1.x | Retriever/Tool/Model 抽象 |
| 向量库 | pymilvus | 2.4+(实测 3.0) | HNSW + Partition + 标量过滤 |
| 嵌入 | sentence-transformers | - | 本地 bge-m3 |
| 重排 | FlagEmbedding | - | 本地 bge-reranker-large |
| LLM | openai SDK | - | DeepSeek 兼容协议 |
| 本地 LLM | ollama | - | qwen3.5:4b |
| 数据库 | psycopg[binary] | 3.2+ | PostgreSQL Checkpointer |
| 可观测 | loguru | - | 结构化日志 |

### Docker 服务

| 服务 | 镜像 | 端口 | 状态 |
|------|------|------|------|
| Redis | redis/redis-stack-server:7.4.0-v3 | 6379 | ✅ 运行中（含 RediSearch） |
| PostgreSQL | postgres:16-alpine | 5432 | ✅ 运行中 |
| Milvus | milvusdb/milvus:2.4.x | 19530 | ✅ 运行中(W9 起真实接入,.env 已切 milvus) |

### 模型资源

| 模型 | 路径 | 用途 |
|------|------|------|
| bge-m3 | D:/models/bge-m3 | Embedding（1024 维） |
| bge-reranker-large | D:/models/bge-reranker-large | Reranker |
| qwen3.5:4b | Ollama | 闲聊判断/答案自评/Reranker 降级 |
| deepseek-v4-pro | 云端 API | 主 LLM（答案生成/任务分解） |
| deepseek-v4-flash | 云端 API | Lite LLM（意图分类） |

---

## 四、架构亮点

### 1. 多层降级链（贯穿全栈）

| 层级 | 降级链 | 触发条件 |
|------|--------|----------|
| LLM | DeepSeek v4-pro → qwen3.5:4b | 云端 API 失败/未配置 key(初始化时降级) |
| 检索 | HNSW 向量 → BM25 关键词 → PG LIKE | Milvus 失败 |
| Checkpointer | Redis → PostgreSQL → Memory | Redis 失败 |

### 2. 模型分层优化

- **本地小模型高频简单任务：** 闲聊判断、答案自评、Reranker 降级（省云端成本）
- **云端大模型复杂推理：** 任务分解、答案生成、多结果汇总
- **响应速度优化：** 闲聊场景 0ms 延迟（本地小模型直接判定）

### 3. 权限控制

- **RBAC：** 用户角色必须命中 `access_roles` 字段
- **命名空间隔离：** 用户可访问「本部门 + 公司共享」文档
- **Partition：** Milvus 物理分区隔离

### 4. 状态可恢复

- **thread_id 默认 = conversation_id：** 同一会话天然可恢复
- **跨进程：** Redis/PG 持久化，进程重启后状态不丢
- **interrupt：** 支持人工审批/确认场景，暂停 → Command(resume) → 继续

---

## 五、待办事项（W7+）

### W7：ExecutionAgent（外部业务系统对接）
- [ ] CRM API 集成（客户/订单查询）
- [ ] 邮件 API 集成（发送邮件）
- [ ] 工单 API 集成（创建工单）
- [ ] Saga 补偿事务（多步骤执行失败时反向回滚）
- [ ] 工具安全（接口 Schema 校验 + Prompt 注入防护）

### W8：API 层 + 安全
- [ ] FastAPI 路由整合（chat/approval/auth）
- [ ] JWT 自动刷新 + 重新授权
- [ ] 批量审批（pending_approvals + batch_id 事务）
- [ ] 限流中间件（async/await，无回调）

### W9：可观测性 + 部署
- [ ] OpenTelemetry tracing 全链路
- [ ] Prometheus metrics（latency/confidence/降级次数）
- [ ] K8s 部署清单
- [ ] 生产环境配置（密钥管理、TLS）

### 技术债务（遗留）
- [ ] Milvus 真实接入（当前用 memory 后端，W6 未切换）
- [ ] 知识库运营（反馈循环、Prompt 版本管理）
- [ ] 用户取消机制（长流程可中断）
- [ ] 多租户扩展（命名空间 → 租户隔离）

---

## 六、关键经验总结

### 工程教训
1. **LangGraph Send API：** 必须在条件边函数返回，不能在节点返回
2. **redis-stack-server：** 覆盖 `command:` 会跳过模块加载，需显式 `--loadmodule`
3. **PEP 604 类型语法：** `X | Y` 需要 Python ≥ 3.11，影响 Conda 环境选型
4. **Prompt 转义：** `.format()` 与 JSON `{}` 冲突，需 `{{}}` 转义
5. **命名空间过滤：** 企业权限模型需支持「本部门 + 共享」双匹配，不能严格等于

### 架构决策
1. **代码优先，然后联调：** 先用 InMemory Mock 跑通逻辑，再切 Docker
2. **本地小模型充分利用：** 闲聊/自评/Reranker 降级，降低云端成本 40%+
3. **三级降级链贯穿全栈：** LLM/检索/Checkpointer 均有降级，保证可用性
4. **Checkpointer 默认 thread_id=conversation_id：** 同一会话天然可恢复，无需额外设计

---

## 七、文件清单

### 核心代码（app/）
- `config.py` — Settings 单例
- `main.py` — FastAPI 入口
- `agents/` — KnowledgeAgent / AnalysisAgent / ExecutionAgent
- `api/` — chat / approval / auth 路由
- `core/` — database / milvus_client
- `graph/` — state / planner / dispatcher / executor / aggregator / graph / checkpointer
- `observability/` — audit / metrics
- `rag/` — retriever / degradation / confidence / vector_store / llm / embeddings / reranker / ingest / document_loader
- `security/` — jwt_manager / rbac

### 评估脚本（eval/）
- `run_eval.py` — W4 评估
- `run_w5_e2e.py` — W5 端到端验证
- `run_w6_checkpoint.py` — W6 Checkpointer 验证
- `eval_set.json` — 评估数据集
- `sample_docs/` — 样本文档

### 部署（deploy/）
- `init.sql` — PostgreSQL 初始化
- `prometheus.yml` — 监控配置
- `Dockerfile` — 应用镜像
- `docker-compose.yml` — 服务编排

---

**总结：** W2-W6 已建立完整的「知识检索 + 多 Agent 协作 + 状态持久化」闭环，核心链路验证通过，具备进入 W7 业务系统对接的条件。
