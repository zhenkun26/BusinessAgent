# 密钥防泄漏(security-hardening-plus 2.1)
# 安装: pip install pre-commit && pre-commit install
# 全量扫描: pre-commit run --all-files
repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.28.0
    hooks:
      - id: gitleaks
        args: [--config=.gitleaks.toml, --verbose]
